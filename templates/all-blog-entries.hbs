{{> header}}
<h1>{{title}}</h1>

{{#each collections.blog}}
<div class="row">
	<div class="col-lg-10">
		<h2><a href="../../{{this.path}}">{{this.title}}</a></h2>
		<p class="small">{{dateFormat this.date "MMMM D, YYYY"}}</p>
		{{this.excerpt}}
	</div>
</div>
{{/each}}

{{> footer}}

